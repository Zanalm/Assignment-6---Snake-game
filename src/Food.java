
public class Food extends GameBoard {
/*	public static void generateFood() {
        int row = (int) (Math.random() * row);
        int column = (int) (Math.random() * column);

        CellNr[row][column];
    }*/
}